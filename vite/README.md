# 🧪 Laboratorio de Bundling con Vite

Repositorio público que implementa un proyecto configurado manualmente con **Vite + TypeScript**, cumpliendo los requisitos del laboratorio de bundling.

---

## 📦 Repositorio

- [ ] El proyecto está subido a un repositorio público (GitHub / GitLab / Bitbucket)

---

## 🚫 Configuración de exclusión (.gitignore)

- [ ] Existe un archivo `.gitignore`
- [ ] La carpeta `node_modules` está excluida
- [ ] La carpeta `dist` está excluida

---

## ⚡ Configuración con Vite (Obligatorio)

### TypeScript

- [❎] El proyecto está configurado con **TypeScript**
- [ ] Los errores de tipos se muestran en la terminal

---

### Scripts de desarrollo

El proyecto incluye los siguientes scripts en `package.json`:

- [ ] `npm start` → levanta el servidor de desarrollo
- [ ] `npm run build` → genera el bundle de producción
- [ ] `npm run preview` → previsualiza la build de producción

---

### Tamaño del bundle

- [ ] Al ejecutar `npm run build` se muestra el tamaño del bundle en consola

---

### Variables de entorno

- [ ] Existen variables de entorno para **desarrollo**
- [ ] Existen variables de entorno para **producción**
- [ ] Se usa `console.log` para mostrar el valor de la variable
- [ ] En `npm start` se muestra el valor de desarrollo
- [ ] En `npm run preview` se muestra el valor de producción

---

### Manipulación del DOM + CSS Modules

- [ ] Se crea un elemento `<h1>` usando la API del DOM
- [ ] El `<h1>` tiene estilos aplicados con **CSS Modules**

---

## ⭐ Opcional — Compresión del bundle

- [ ] La build genera archivos comprimidos **GZIP**
- [ ] La build genera archivos comprimidos **BROTLI**
- [ ] Existen archivos `.js.gz` en la carpeta `dist`
- [ ] Existen archivos `.js.br` en la carpeta `dist`

---

## 📁 Estructura del proyecto (referencia)
